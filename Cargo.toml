[package]
name = "async-usb-host"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
defmt = { version = "0.3.8", optional = true }
static_assertions = "0.1.0"

[features]
defmt = ["dep:defmt"]
